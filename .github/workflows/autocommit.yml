name: Auto commit

on:

  push:
    branches:
      - master
      
  schedule:
  - cron: "0 7,8,9,10,11 * * *"

jobs:
  auto_commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2      
        with:
         persist-credentials: false
         fetch-depth: 0

      - name: Modify last update
        run: |
          cat > LAST_UPDATED
          d=`date '+%Y-%m-%dT%H:%M:%SZ'`
          echo $d > LAST_UPDATED
          
      - name: Commit changes
        run: |
          git config --local user.email "akbarfattahul22@gmail.com"
          git config --local user.name "fattakbar"
          git add -A
          
          arr[0]="feat: 😂 update"
          arr[1]="feat: 😱 update"
          arr[2]="feat: 👿 update"
          arr[3]="feat: 💩 update"
          arr[4]="feat: 🙏 update"
          arr[5]="feat: 🙈 update"
          arr[6]="feat: 🐐 update"
          arr[7]="feat: 🚀 update"
          
          rand=$[$RANDOM % ${#arr[@]}]
          
          git commit -m "${arr[$rand]}"
          
      - name: GitHub Push
        uses: ad-m/github-push-action@v0.5.0
        with:
          force: true
          directory: "."
          github_token: ${{ secrets.GITHUB_TOKEN }}
